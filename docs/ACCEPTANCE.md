# 验收标准

## 工程阶段

- npm workspace 包含 Web、API 和共享包。
- 所需目录、依赖、脚本、严格 TypeScript 与基础规则存在。
- `npm run check` 依次完成 lint、typecheck、unit test 和 build。
- 不包含剧情、地图、POI 或页面业务实现。

## 后续阶段

功能、内容、兼容性、性能和上线验收标准待相应阶段补充。
